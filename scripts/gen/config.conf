# 脚本读取文件文本内容，配置文件可以是任何类型
# 可以通过 yarn gen ./your-config.txt 方式，指定一个相对于项目跟目录中的配置文件
# ######所包裹的内容是用来区分配置模块的，不要随意改动， 配置块的顺序可以随意调整
# columns配置有三个来源，优先级为：接口 > 数据库表 > 当前配置文件
# 只要接口配置中 url 或者 数据库配置总 tableName 行被注释掉，就不会使用接口或者数据库表生成代码
# 三种注释方式 # ; //

###### 数据库配置 ######
#各式 mysql://user:password@host:port/database?querystring
url  mysql://fd:123456@172.16.60.247:3306/visible_omponent_dev?useUnicode=true&characterEncoding=UTF-8&autoReconnect=true&zeroDateTimeBehavior=convertToNull
tableName user

###### 接口配置 ######
; url https://172.16.170.48:2233//v2/api-docs
; url http://localhost:8080/v2/api-docs
userName admin # 可能不需要
password 123456 # 可能不需要
# 具体功能对应的接口在《基础配置》中填写

###### 基础配置 #####
目录 user-center  # 全部在pages下创建
; 路由 /user-center # 默认与目录相同
## 请求配置 默认将使用"目录"基于RestFul风格生成
## 请求类型 method url field1 field2 ... 多个忽略字段不参与代码生成
查询 get /user/all # 能够获取到查询条件、表格信息
修改 put /user # 能够获取到编辑表单信息
# 以下默认根据"查询"或者"目录" 基于RestFul风格生成
; 添加 post /user
; 删除 delete /user/{id}
; 详情 get /user/{id}
; 批量删除 delete /user

###### 页面类型配置 列表页、弹框编辑、页面编辑 系统默认模板和文件名，可以不指定模板和文件名。模板路径从根目录开始写起######
列表页面
弹框表单
; 页面表单
; 自定义页面 /src/template.js->customer.jsx # 自定义页面需要同时指定模板和目标文件名

###### 查询条件配置 也可以从表格列配置中提取 ######
# 可用类型：input hidden number textarea password mobile email select select-tree checkbox checkbox-group radio radio-group switch date time date-time date-range cascader json icon-picker
用户名 userName input
年龄 age number
工作 job

###### 工具条配置 ######
添加
删除
; 导入
; 导出
; 添加任务 handleAddTask # icon 可缺省
; 测试 handleTest1 plus # 如果要指定icon handle函数不可缺省

###### 表格配置 ######
可选中
分页
序号

###### 表格列配置 作为查询条件(query):q  作为表单(form):f 是否必填（required）:r ######
用户名 userName q f r
年龄 age number q
工作 job select
职位 position select q
测试配置文件 test f r

###### 操作列配置 ######
图标模式 # 开启之后 操作列将只显示图标

# 以下为操作按钮配置
修改
删除
; 修改任务 handleEditTask # icon 可缺省
; 示例 handleTest plus # 如果要指定icon handle函数不可缺省

###### 表单元素配置 也可以从表格列配置中提取 是否必填（required）:r ######
用户名 userName input r
年龄 age number r
