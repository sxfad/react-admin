import {FileImageOutlined} from '@ant-design/icons';
import React from 'react';

export default {
    icon: <FileImageOutlined/>,
    isContainer: false,

    fields: [
        {label: '网页地址', field: 'src', type: 'string'},
    ],
};
